# Frontend E2E

These Playwright tests run against the Vite dev server and mock `/api/v1/**` responses in the browser. They verify frontend routing and critical user-facing flows without requiring a live backend database.

Run:

```powershell
npm run test:e2e
```

Open the interactive runner:

```powershell
npm run test:e2e:ui
```
